<h1>Users#index</h1>

<table>
  <tr>
    <th>Image</th>
    <th>Name</th>
    <th>Email</th>
  </tr>
<% @users.each do |user| %>
  <tr>
    <td><%= user.avatar.url(:thumb) %></td>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
  </tr>
<% end %>
</table>